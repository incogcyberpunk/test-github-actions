events{
}

http{
    
   include /etc/nginx/mime.types;

   upstream backend {
        server localhost:8080;
        server localhost:3333;
   }

    server {
        listen 80;

        location / {
            proxy_pass http://backend;
        }
    }
     
}
